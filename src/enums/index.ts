/**
 * Service枚举
 */
export enum ServiceEnum {
  /**
   * logo
   */
  LogoUrl = "/logo.svg",
  /**
   * 默认头像
   */
  AvatarDefault = "/avatar.png",
  /**
   * System 路径
   */
  SystemPath = "/system"
}

/**
 * useAggregation 所使用的的枚举类
 */
export enum UseKey {
  Event="event",
  UserInfo = "userInfo"
}
